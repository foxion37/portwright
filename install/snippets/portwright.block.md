<!-- portwright:start (managed block — safe to remove between these markers) -->
## portwright
Before connecting, using, troubleshooting, or instructing any external tool, run `{{PORTWRIGHT_HOME}}/bin/portwright preflight <service-id>{{CONTENT_HOME_FLAG}}` and follow its Profile, Procedure, Lessons, freshness, and advisory tier (`confirm`/`forbid` go through your own permission prompt).
Load the full guide once per session with `{{PORTWRIGHT_HOME}}/bin/portwright skills get portwright-tool-use`; load `portwright-tool-memory` only before drafting or promoting memory. Never write durable memory directly.
<!-- portwright:end -->
